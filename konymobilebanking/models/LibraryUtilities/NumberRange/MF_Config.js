define([],function(){
	var mappings = {
	};
	Object.freeze(mappings);
	
	var typings = {
	}
	Object.freeze(typings);
	
	var primaryKeys = [
	];
	Object.freeze(primaryKeys);
	
	var config = {
		mappings : mappings,
		typings : typings,
		primaryKeys : primaryKeys,
		serviceName : "LibraryUtilities",
		tableName : "NumberRange"
	};
	Object.freeze(config);
	
	return config;
})
